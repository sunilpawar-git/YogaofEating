import Foundation

/// Backward-compatibility bridge. All production code must use `DailyInsight` directly.
/// Deleted in Phase 5 alongside all other legacy insight aliases.
typealias EnrichedDailyInsight = DailyInsight
